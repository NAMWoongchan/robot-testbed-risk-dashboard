# 공동 수업 개선 검증 기록

검사일: 2026-09-08 (KST). 대상: feature/classroom 개발 브랜치. **공개 서비스 공동 편집 배포 완료 기록이 아닙니다.**

## 환경과 검증 경로
- Windows, Microsoft Edge 152.0.4191.66, Playwright의 독립 BrowserContext 2개.
- 개인 모드: file://, 데스크톱 및 390px 모바일.
- 공동 모드: 127.0.0.1 임시 HTTP 서버 + 실제 Firebase JS SDK 12.18.0 + Firestore Emulator 1.22.0, demo-classroom.
- Browser 플러그인의 필수 Node REPL 도구가 없어 기존 Playwright 검사 경로를 사용했습니다.
- 에뮬레이터는 테스트 전용 가상 UID를 사용합니다. 실제 GitHub OAuth 로그인 성공의 근거는 아닙니다.
- 외부 SDK를 불러오는 최초 제한 환경 실행은 시간 초과했습니다. 네트워크 접근이 허용된 격리 브라우저로 재실행해 통과했습니다.
- 리스크 앱의 공동 데이터 어댑터 연결 누락을 발견하고 수정한 뒤 관련 검사를 재실행했습니다.

## 실제 결과
| 검사 | 이슈 보드 | 리스크 보드 |
|---|---|---|
| 기존 개인 모드 브라우저 검사 | 15개 통과 | 10개 통과 |
| 계산·입력 검증 단위 검사 | 기존 브라우저 검사에 포함 | 7개 통과 |
| 서버 권한·입력 규칙 | 에뮬레이터 통과 | 에뮬레이터 통과 |
| 참가자 A 등록 → 참가자 B 실시간 반영 | 통과 | 통과 |
| 동일 항목 동시 수정 시 이전 버전 저장 거부 | 통과 | 통과 |
| 새로고침 후 같은 방 재참가 → 서버 자료 유지 | 통과 | 통과 |
| 삭제의 다른 참가자 화면 반영 | 통과 | 통과 |
| 강사 마감 후 학생 저장 거부 | 통과 | 통과 |
| 개인 모드 복귀와 기존 localStorage 불변 | 통과 | 통과 |
| 공동 수업 전체 JSON 복원 차단 | 해당 기능 없음 | 통과 |
| 모바일 가로 넘침·페이지 오류·콘솔 오류·HTTP 400 이상 | 넘침 없음·오류 0건 | 넘침 없음·오류 0건 |
| 실제 GitHub OAuth 로그인 | 미검증 | 미검증 |
| 실제 Firebase 프로젝트·배포된 보안 규칙 | 미검증 | 미검증 |
| 공개 Pages 사이트 공동 편집 | 미배포·미검증 | 미배포·미검증 |
| 수십 명 동시 부하·무료 할당량 적합성 | 미검증 | 미검증 |
| 오프라인 전환/복구·장시간 세션·강사 마감 후 재개 UI | 미검증 | 미검증 |

보안 규칙 검사는 미로그인, 미참가, 다른 교육방, 학생의 강사 권한 획득, 빈/공백 제목, 잘못된 상태·점수 범위, 이전 버전, 마감 후 저장을 실제 요청으로 거부하는지 확인했습니다. 규칙 검사의 PERMISSION_DENIED 로그는 의도한 차단 결과입니다.

## 근거 파일
- 재현 코드: `tests/classroom.integration.cjs`, 각 저장소의 기존 `tests/browser-test.cjs`.
- 워크스페이스 외부 증거: `../classroom-qa-shared-issue-final/results.json`, `../classroom-qa-shared-risk/results.json`, 각 폴더의 mobile.png.
- 개인 모드 재검사: `../classroom-qa-issue-final/`, `../classroom-qa-risk-final/`.
- 실제 설정 단계와 제한사항: [CLASSROOM.md](CLASSROOM.md).

스크린샷·다운로드 백업·테스트 도구·로컬 저장 데이터는 Git에 포함하지 않습니다. 비밀키 패턴 검사는 선택한 소스에 한정하며, 비밀이 없다는 절대적인 보증은 아닙니다.
